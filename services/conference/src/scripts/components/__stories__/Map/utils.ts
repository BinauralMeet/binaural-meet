import {makeStyles} from '@material-ui/core'

export const useBaseStyles = makeStyles({
  root: {
    width: '80vw',
    height: '80vh',
    overflow: 'hidden',
    border: '1px solid black',
  },
})
